# 随机起名功能 - 技术方案

## 📋 概述

为 Noveler 添加智能随机起名功能，支持多种风格的姓名生成，帮助作者快速创建人物角色。

**功能定位**: 提高创作效率，解决"起名困难症"

**优先级**: ⭐⭐⭐⭐ 高

**工作量**: 6-10 小时

**收益**: 用户体验大幅提升，降低创作门槛

---

## 🎯 功能需求

### 核心功能

1. **中文姓名生成**
   - 现代风格（常见姓氏 + 现代名字）
   - 古典风格（单字名、双字名、雅致字）
   - 奇幻风格（仙侠、玄幻命名规则）
   - 历史风格（朝代特色姓名）

2. **英文姓名生成**
   - 基于真实数据的英文名（First Name + Last Name）
   - 支持性别选择（男性/女性/随机）

3. **日文姓名生成**
   - 常见日本姓氏 + 名字组合
   - 支持性别选择

4. **奇幻/虚构姓名**
   - 基于音节组合生成
   - 可配置长度（2-4 音节）
   - 适用于魔法、科幻等题材

### 交互方式

1. **命令面板触发**
   - `Noveler: 随机起名` 命令
   - 显示风格选择菜单

2. **快捷按钮**（可选）
   - 在人物管理侧边栏添加"随机起名"按钮
   - 创建人物时提供"随机生成"选项

3. **结果展示**
   - 一次生成 5-10 个备选名字
   - 支持"再来一组"按钮
   - 可直接复制到剪贴板
   - 可直接创建人物档案

---

## 🏗️ 技术架构

### 1. 文件结构

```
src/
├── services/
│   └── nameGeneratorService.ts    # 核心生成服务
├── commands/
│   └── generateName.ts             # 命令入口
├── data/
│   ├── chinese-surnames.json       # 中文姓氏库
│   ├── chinese-given-names.json    # 中文名字库
│   ├── fantasy-syllables.json      # 奇幻音节库
│   └── name-patterns.json          # 命名规则配置
└── utils/
    └── nameValidator.ts            # 名字验证工具
```

### 2. 核心类设计

#### NameGeneratorService

```typescript
/**
 * 姓名生成服务
 */
export class NameGeneratorService {
    private chineseSurnames: string[];
    private chineseGivenNames: Record<string, string[]>;
    private fantasySyllables: string[];

    /**
     * 生成中文姓名
     * @param style 风格：modern | classic | fantasy | historical
     * @param count 生成数量
     * @returns 姓名数组
     */
    generateChineseName(
        style: ChineseNameStyle,
        count: number = 5
    ): string[];

    /**
     * 生成英文姓名
     * @param gender 性别：male | female | random
     * @param count 生成数量
     * @returns 姓名数组
     */
    generateEnglishName(
        gender?: 'male' | 'female' | 'random',
        count: number = 5
    ): string[];

    /**
     * 生成日文姓名
     * @param gender 性别
     * @param count 生成数量
     * @returns 姓名数组
     */
    generateJapaneseName(
        gender?: 'male' | 'female' | 'random',
        count: number = 5
    ): string[];

    /**
     * 生成奇幻/虚构姓名
     * @param length 音节长度（2-4）
     * @param count 生成数量
     * @returns 姓名数组
     */
    generateFantasyName(
        length: number = 3,
        count: number = 5
    ): string[];
}

/**
 * 中文姓名风格
 */
export type ChineseNameStyle = 'modern' | 'classic' | 'fantasy' | 'historical';
```

---

## 📦 数据结构

### 1. 中文姓氏库 (chinese-surnames.json)

```json
{
  "common": [
    "李", "王", "张", "刘", "陈", "杨", "黄", "赵", "周", "吴",
    "徐", "孙", "马", "朱", "胡", "郭", "何", "林", "罗", "高"
  ],
  "classic": [
    "司马", "欧阳", "上官", "诸葛", "公孙", "轩辕", "慕容", "东方"
  ],
  "fantasy": [
    "凌", "云", "墨", "夜", "冷", "独孤", "南宫", "司徒"
  ]
}
```

### 2. 中文名字库 (chinese-given-names.json)

```json
{
  "modern": {
    "male": ["浩然", "子轩", "俊杰", "宇轩", "博文"],
    "female": ["诗涵", "雨萱", "欣怡", "思琪", "梦琪"]
  },
  "classic": {
    "single": ["云", "风", "月", "雪", "霜", "雨", "星", "辰"],
    "double": ["青云", "明月", "清风", "流云", "晓雪"]
  },
  "fantasy": {
    "prefix": ["天", "玄", "灵", "幽", "星", "月", "剑", "魔"],
    "suffix": ["尘", "羽", "辰", "霄", "渊", "影", "心", "魂"]
  }
}
```

### 3. 奇幻音节库 (fantasy-syllables.json)

```json
{
  "consonants": ["b", "d", "f", "g", "k", "l", "m", "n", "r", "s", "t", "v", "z"],
  "vowels": ["a", "e", "i", "o", "u", "ae", "ei"],
  "patterns": [
    "{consonant}{vowel}{consonant}",
    "{consonant}{vowel}",
    "{vowel}{consonant}"
  ]
}
```

---

## 🔧 实现细节

### 1. 中文姓名生成算法

#### 现代风格
```typescript
function generateModernChineseName(): string {
    const surname = randomPick(chineseSurnames.common);
    const givenName = randomPick(chineseGivenNames.modern[randomGender()]);
    return surname + givenName;
}
```

#### 古典风格
```typescript
function generateClassicChineseName(): string {
    const surname = randomPick([
        ...chineseSurnames.common,
        ...chineseSurnames.classic
    ]);

    // 70% 概率双字名，30% 概率单字名
    const useDouble = Math.random() > 0.3;

    if (useDouble) {
        const givenName = randomPick(chineseGivenNames.classic.double);
        return surname + givenName;
    } else {
        const givenName = randomPick(chineseGivenNames.classic.single);
        return surname + givenName;
    }
}
```

#### 奇幻风格
```typescript
function generateFantasyChineseName(): string {
    const surname = randomPick([
        ...chineseSurnames.fantasy,
        ...chineseSurnames.classic
    ]);

    const prefix = randomPick(chineseGivenNames.fantasy.prefix);
    const suffix = randomPick(chineseGivenNames.fantasy.suffix);

    return surname + prefix + suffix;
}
```

### 2. 英文姓名生成（使用 Faker.js）

```typescript
import { faker } from '@faker-js/faker';

function generateEnglishName(gender?: 'male' | 'female'): string {
    if (gender === 'male') {
        return faker.person.fullName({ sex: 'male' });
    } else if (gender === 'female') {
        return faker.person.fullName({ sex: 'female' });
    } else {
        return faker.person.fullName();
    }
}
```

### 3. 奇幻姓名生成

```typescript
function generateFantasyName(syllableCount: number = 3): string {
    const syllables: string[] = [];

    for (let i = 0; i < syllableCount; i++) {
        const pattern = randomPick(fantasySyllables.patterns);
        const syllable = pattern
            .replace('{consonant}', randomPick(fantasySyllables.consonants))
            .replace('{vowel}', randomPick(fantasySyllables.vowels));
        syllables.push(syllable);
    }

    // 首字母大写
    const name = syllables.join('');
    return name.charAt(0).toUpperCase() + name.slice(1);
}
```

---

## 🎨 用户交互流程

### 流程 1: 命令面板

```
1. 用户按下 Cmd/Ctrl + Shift + P
2. 输入 "Noveler: 随机起名"
3. 显示风格选择菜单：
   ├─ 中文姓名（现代）
   ├─ 中文姓名（古典）
   ├─ 中文姓名（奇幻）
   ├─ 中文姓名（历史）
   ├─ 英文姓名
   ├─ 日文姓名
   └─ 虚构姓名
4. 选择风格后，显示生成结果 QuickPick：
   ├─ 姓名1
   ├─ 姓名2
   ├─ ...
   ├─ 姓名10
   ├─ [再来一组]
   └─ [取消]
5. 选择姓名后，复制到剪贴板并提示成功
```

### 流程 2: 侧边栏快捷按钮（可选）

```
人物管理
  ├─ 张三
  ├─ 李四
  └─ [➕ 创建人物]  [🎲 随机起名]
       ↓                  ↓
   创建对话框      显示风格菜单 → 显示结果 → 创建人物
```

---

## 📚 词库来源策略

### 核心原则
- **精选而非穷举**：每个类别 50-200 个精选条目即可，覆盖常见使用场景
- **手工维护**：确保质量，避免自动爬取的低质量数据
- **开源友好**：使用公开数据或自行整理

### 1. 中文姓氏（约 100 个）

#### 来源策略
1. **百家姓前 50**：李、王、张、刘、陈等常见姓氏
   - 参考：维基百科《百家姓》条目
   - 覆盖率：约占人口 50%

2. **复姓（10-15 个）**：司马、欧阳、上官、诸葛等
   - 参考：《中国姓氏大辞典》公开资料
   - 用途：古典/历史风格

3. **奇幻姓氏（20-30 个）**：手工创作
   - 单字：凌、墨、夜、冷、萧、云
   - 复姓：独孤、南宫、司徒、慕容

#### 实施方案
```typescript
// 手工整理到 data/chinese-surnames.json
{
  "common": [
    "李", "王", "张", "刘", "陈", "杨", "黄", "赵",
    "周", "吴", "徐", "孙", "马", "朱", "胡", "郭",
    "何", "高", "林", "罗", "郑", "梁", "谢", "宋"
    // ... 共 50 个
  ],
  "classic": [
    "司马", "欧阳", "上官", "诸葛", "公孙", "轩辕",
    "东方", "西门", "南宫", "北堂"
    // ... 共 15 个
  ],
  "fantasy": [
    "凌", "墨", "夜", "冷", "萧", "云", "风", "龙",
    "独孤", "南宫", "司徒", "慕容", "皇甫", "百里"
    // ... 共 30 个
  ]
}
```

### 2. 中文名字（约 200 个）

#### 来源策略
1. **现代常见名**：
   - 来源：公安部《全国姓名报告》（公开数据）
   - 男性 TOP 30：浩然、子轩、宇轩等
   - 女性 TOP 30：诗涵、雨萱、欣怡等

2. **古典诗意名**：手工整理
   - 单字（50 个）：云、风、月、雪、霜、雨、星、辰、玉、琴
   - 双字（50 个）：青云、明月、清风、流云、晓雪

3. **奇幻武侠名**：手工创作
   - 前缀（20 个）：天、玄、灵、幽、星、月、剑、魔
   - 后缀（20 个）：尘、羽、辰、霄、渊、影、心、魂

#### 实施方案
```typescript
// data/chinese-given-names.json
{
  "modern": {
    "male": [
      "浩然", "子轩", "宇轩", "俊杰", "博文", "雨泽",
      "文博", "昊然", "梓豪", "子墨"
      // ... 共 30 个
    ],
    "female": [
      "诗涵", "雨萱", "欣怡", "思琪", "梦琪", "雅雯",
      "语嫣", "婉儿", "若兮", "依然"
      // ... 共 30 个
    ]
  },
  "classic": {
    "single": [
      "云", "风", "月", "雪", "霜", "雨", "星", "辰",
      "玉", "琴", "竹", "梅", "兰", "菊"
      // ... 共 50 个
    ],
    "double": [
      "青云", "明月", "清风", "流云", "晓雪", "寒梅",
      "冷月", "秋水", "春雨", "夏荷"
      // ... 共 50 个
    ]
  },
  "fantasy": {
    "prefix": [
      "天", "玄", "灵", "幽", "星", "月", "剑", "魔",
      "圣", "冥", "血", "暗", "神", "龙"
      // ... 共 20 个
    ],
    "suffix": [
      "尘", "羽", "辰", "霄", "渊", "影", "心", "魂",
      "煞", "狼", "虎", "龙", "凤", "麟"
      // ... 共 20 个
    ]
  }
}
```

### 3. 日文姓名（约 150 个）

#### 来源策略
1. **常见日本姓氏（50 个）**：
   - 参考：日本厚生劳动省《姓氏分布统计》
   - 佐藤、铃木、高桥、田中、渡边等

2. **常见日本名字**：
   - 男性（50 个）：太郎、健、翔、优太、大辉
   - 女性（50 个）：樱、美咲、爱、优奈、结衣

#### 实施方案
```typescript
// data/japanese-names.json
{
  "surnames": [
    "佐藤", "铃木", "高桥", "田中", "渡边", "伊藤",
    "山本", "中村", "小林", "加藤"
    // ... 共 50 个
  ],
  "givenNames": {
    "male": [
      "太郎", "健", "翔", "优太", "大辉", "拓海",
      "悠斗", "阳向", "陆", "树"
      // ... 共 50 个
    ],
    "female": [
      "樱", "美咲", "爱", "优奈", "结衣", "阳菜",
      "花", "美羽", "心春", "葵"
      // ... 共 50 个
    ]
  }
}
```

### 4. 英文姓名

#### 来源策略
**使用 @faker-js/faker 库**：
- 优势：内置 10000+ 真实英文姓名数据
- 来源：美国人口普查局公开数据
- 无需手工维护

```typescript
import { faker } from '@faker-js/faker';

// 内置数据，无需额外维护
faker.person.firstName('male');   // "John"
faker.person.lastName();           // "Smith"
faker.person.fullName();           // "John Smith"
```

### 5. 奇幻音节（约 50 个）

#### 来源策略
**手工设计语音规则**：
- 辅音（20 个）：b, d, f, g, k, l, m, n, r, s, t, v, z, th, sh, ch
- 元音（10 个）：a, e, i, o, u, ae, ei, ou, ia, io
- 音节模式：CV、CVC、VC（C=辅音，V=元音）

#### 实施方案
```typescript
// data/fantasy-syllables.json
{
  "consonants": [
    "b", "d", "f", "g", "k", "l", "m", "n", "r", "s",
    "t", "v", "z", "th", "sh", "ch", "kr", "tr", "dr", "gr"
  ],
  "vowels": [
    "a", "e", "i", "o", "u",
    "ae", "ei", "ou", "ia", "io"
  ],
  "patterns": [
    "{consonant}{vowel}",
    "{consonant}{vowel}{consonant}",
    "{vowel}{consonant}"
  ]
}
```

---

## 🎯 词库质量控制

### 1. 来源标准
- ✅ 使用公开数据（统计局、维基百科、厚生劳动省）
- ✅ 手工精选，确保无不雅谐音
- ✅ 适合小说创作场景
- ❌ 不使用网络爬虫（质量无法保证）
- ❌ 不使用机器生成（可能出现奇怪组合）

### 2. 审核流程
1. **初步筛选**：排除生僻字、不雅谐音
2. **人工审核**：确保文化适配性
3. **测试验证**：生成 1000 个样本，检查合理性
4. **社区反馈**：上线后根据用户反馈调整

### 3. 数据文件大小控制
- 每个 JSON 文件 < 10KB
- 总数据量 < 50KB
- 打包后对扩展体积影响 < 0.1%

---

## 📦 数据文件组织

```
src/data/
├── chinese-surnames.json      # 5KB  - 中文姓氏（100 个）
├── chinese-given-names.json   # 10KB - 中文名字（200 个）
├── japanese-names.json        # 8KB  - 日文姓名（150 个）
├── fantasy-syllables.json     # 2KB  - 奇幻音节（50 个）
└── name-patterns.json         # 3KB  - 命名规则配置
                               ────
                               28KB 总计
```

---

## 🔄 词库更新策略

### 初期（v1.0）
- 手工整理精选词库（28KB）
- 覆盖 80% 常见使用场景

### 中期（v1.1+）
- 根据用户反馈添加/调整词条
- 增加区域特色词库（如南方姓氏、北方姓氏）

### 长期（v2.0+）
- 支持用户自定义词库
- 社区贡献词库（GitHub 收集）
- AI 智能生成（基于小说背景）

---

## 📚 参考资料

### 公开数据来源
1. **中文姓氏**
   - 维基百科：《百家姓》
   - 公安部：《2020 年全国姓名报告》（公开摘要）

2. **中文名字**
   - 公安部：《2019 年全国姓名报告》
   - 起名网公开数据（常见名字统计）

3. **日文姓名**
   - 维基百科：《日本人姓氏列表》
   - 日本命名排行榜（公开数据）

4. **英文姓名**
   - @faker-js/faker 库（美国人口普查数据）

### 手工整理部分
- 古典诗意名字：基于诗词、成语、典故
- 奇幻武侠名字：参考经典小说（金庸、古龙等）
- 音节规则：基于语音学基础规则

---

## 📝 数据示例

### 中文姓氏库完整示例（100 个）

```json
{
  "common": [
    "李", "王", "张", "刘", "陈", "杨", "黄", "赵", "周", "吴",
    "徐", "孙", "马", "朱", "胡", "郭", "何", "高", "林", "罗",
    "郑", "梁", "谢", "宋", "唐", "许", "韩", "冯", "邓", "曹",
    "彭", "曾", "萧", "田", "董", "袁", "潘", "于", "蒋", "蔡",
    "余", "杜", "叶", "程", "苏", "魏", "吕", "丁", "任", "沈"
  ],
  "classic": [
    "司马", "欧阳", "上官", "诸葛", "公孙", "轩辕", "慕容", "东方",
    "西门", "南宫", "北堂", "令狐", "皇甫", "尉迟", "澹台"
  ],
  "fantasy": [
    "凌", "墨", "夜", "冷", "萧", "云", "风", "龙", "剑", "魔",
    "独孤", "南宫", "司徒", "慕容", "皇甫", "百里", "端木", "东方",
    "北冥", "西楚", "云天", "月影", "星辰", "九幽", "无双"
  ]
}
```

---

## 🚀 实施步骤更新

### Phase 0: 词库准备（2 小时）**新增**

1. ✅ 整理中文姓氏库（100 个）
2. ✅ 整理中文名字库（200 个）
3. ✅ 整理日文姓名库（150 个）
4. ✅ 整理奇幻音节库（50 个）
5. ✅ 质量审核和测试生成

### Phase 1: 基础功能（4 小时）

1. ✅ 创建 NameGeneratorService 类
2. ✅ 实现中文姓名生成（现代风格）
3. ✅ 加载和解析词库数据
4. ✅ 注册命令和基本 UI

### Phase 2: 多风格支持（2 小时）

1. ✅ 实现古典、奇幻、历史风格
2. ✅ 集成 @faker-js/faker 生成英文姓名
3. ✅ 实现日文姓名生成

### Phase 3: 增强功能（2 小时）

1. ✅ 添加"再来一组"功能
2. ✅ 集成到人物创建流程
3. ✅ 添加侧边栏快捷按钮
4. ✅ 优化交互体验

### Phase 4: 测试与优化（2 小时）

1. ✅ 编写单元测试
2. ✅ 用户测试和反馈
3. ✅ 性能优化
4. ✅ 文档完善

**总工作量：12 小时**（原 10 小时 + 词库准备 2 小时）

---

## 📚 依赖库

### 1. @faker-js/faker

**用途**: 生成英文姓名

```bash
npm install @faker-js/faker
```

**使用示例**:
```typescript
import { faker } from '@faker-js/faker';

// 男性姓名
faker.person.fullName({ sex: 'male' });
// => "John Smith"

// 女性姓名
faker.person.fullName({ sex: 'female' });
// => "Jane Doe"

// 随机姓名
faker.person.fullName();
// => "Alex Johnson"
```

### 2. 无额外依赖（中文/日文/奇幻）

中文、日文和奇幻姓名生成使用内置数据和算法，无需第三方库。

---

## 🧪 测试计划

### 单元测试

```typescript
describe('NameGeneratorService', () => {
    let service: NameGeneratorService;

    beforeEach(() => {
        service = new NameGeneratorService();
    });

    test('生成中文现代姓名', () => {
        const names = service.generateChineseName('modern', 5);
        expect(names).toHaveLength(5);
        names.forEach(name => {
            expect(name).toMatch(/^[\u4e00-\u9fa5]{2,4}$/); // 2-4 个汉字
        });
    });

    test('生成英文姓名', () => {
        const names = service.generateEnglishName('male', 5);
        expect(names).toHaveLength(5);
        names.forEach(name => {
            expect(name).toMatch(/^[A-Za-z\s]+$/);
        });
    });

    test('生成奇幻姓名', () => {
        const names = service.generateFantasyName(3, 5);
        expect(names).toHaveLength(5);
        names.forEach(name => {
            expect(name.length).toBeGreaterThan(3);
            expect(name[0]).toMatch(/[A-Z]/); // 首字母大写
        });
    });
});
```

### 集成测试

- 测试命令注册和调用
- 测试 QuickPick 交互流程
- 测试剪贴板复制功能
- 测试与人物创建的集成

---

## 📈 性能考虑

### 1. 数据加载

- **懒加载**: 首次使用时才加载姓名数据
- **缓存**: 加载后缓存在内存中，避免重复读取
- **数据大小**: 控制每个 JSON 文件在 10KB 以内

```typescript
export class NameGeneratorService {
    private static instance: NameGeneratorService;
    private dataLoaded = false;

    private async ensureDataLoaded() {
        if (this.dataLoaded) return;

        // 懒加载数据
        await this.loadData();
        this.dataLoaded = true;
    }
}
```

### 2. 生成性能

- 单次生成 10 个姓名，耗时 < 10ms
- 使用简单随机算法，避免复杂计算

---

## 🔒 数据质量保证

### 1. 名字合理性

- **过滤生僻字**: 避免使用过于生僻的汉字
- **避免谐音**: 人工审核常见姓名组合，避免不雅谐音
- **文化适配**: 遵循各语言的命名习惯

### 2. 重复检测

```typescript
function generateUniqueNames(count: number): string[] {
    const names = new Set<string>();

    while (names.size < count) {
        const name = generateSingleName();
        names.add(name);
    }

    return Array.from(names);
}
```

---

## 🚀 实施步骤

### Phase 1: 基础功能（4 小时）

1. ✅ 创建 NameGeneratorService 类
2. ✅ 实现中文姓名生成（现代风格）
3. ✅ 准备中文姓氏和名字数据
4. ✅ 注册命令和基本 UI

### Phase 2: 多风格支持（2 小时）

1. ✅ 实现古典、奇幻、历史风格
2. ✅ 集成 Faker.js 生成英文姓名
3. ✅ 实现日文姓名生成

### Phase 3: 增强功能（2 小时）

1. ✅ 添加"再来一组"功能
2. ✅ 集成到人物创建流程
3. ✅ 添加侧边栏快捷按钮
4. ✅ 优化交互体验

### Phase 4: 测试与优化（2 小时）

1. ✅ 编写单元测试
2. ✅ 用户测试和反馈
3. ✅ 性能优化
4. ✅ 文档完善

---

## 📝 配置支持（可选）

允许用户自定义姓名库：

```jsonc
// novel.jsonc
{
  "noveler": {
    "nameGenerator": {
      "enabled": true,
      "customSurnames": ["自定义", "姓氏"],
      "customGivenNames": ["自定义", "名字"]
    }
  }
}
```

---

## 🎯 成功指标

- ✅ 支持 4 种中文风格 + 英文 + 日文 + 奇幻
- ✅ 单次生成 < 100ms
- ✅ 姓名合理性 > 95%
- ✅ 无明显不雅谐音
- ✅ 用户满意度 > 90%

---

## 🔮 未来扩展

1. **AI 生成**：基于小说背景和角色设定生成定制姓名
2. **名字含义**：显示姓名的文化含义和寓意
3. **姓名风格分析**：分析现有角色姓名风格，生成匹配的新姓名
4. **名字冲突检测**：避免与已有角色重名
5. **多语言支持**：韩文、俄文、西班牙文等

---

**创建时间**: 2025-12-08
**预计完成时间**: 1-2 天
**优先级**: 高
**状态**: 待实施
