# VSCode 项目配置说明

## 📋 概述

Noveler 会在初始化项目时自动创建 `.vscode/settings.json` 文件，包含适合小说写作的推荐配置。

**重要**: 这些配置**仅对当前小说项目生效**，不会影响你的其他 VSCode 项目。

---

## ✅ 默认配置

### 自动保存

```json
"files.autoSave": "afterDelay",
"files.autoSaveDelay": 2000
```

**作用**: 输入停止 2 秒后自动保存，防止意外丢失写作内容。

**适合场景**:
- ✅ 小说写作（连续输入，偶尔停顿思考）
- ✅ 长时间写作会话

**如需调整**:
- 缩短延迟: `"files.autoSaveDelay": 1000` (1秒)
- 关闭自动保存: `"files.autoSave": "off"`

---

### 文件格式化

```json
"files.trimTrailingWhitespace": true,
"files.insertFinalNewline": true
```

**作用**:
- 保存时自动删除行尾多余空格
- 文件末尾添加换行符（符合 Unix 标准）

**好处**:
- 文件更整洁
- Git 版本控制更友好

---

### Markdown 预览

```json
"markdown.preview.breaks": true,
"markdown.preview.typographer": true
```

**作用**:
- `breaks`: 预览时单个换行符也会换行（适合中文段落）
- `typographer`: 智能标点（如 `--` 转为 `—`）

---

### 文件隐藏

```json
"files.exclude": {
  "**/.DS_Store": true,
  "**/.noveler": true
},
"search.exclude": {
  "**/.noveler": true
}
```

**作用**:
- 隐藏系统文件和 Noveler 内部配置
- 搜索时排除这些目录

---

## 🎛️ 自定义配置

### 场景 1: 不喜欢自动保存

编辑 `.vscode/settings.json`:

```json
{
  "files.autoSave": "off"
}
```

此时需要手动 `Cmd/Ctrl + S` 保存，或启用 VSCode 全局自动保存。

---

### 场景 2: 想要更频繁的自动保存

```json
{
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 500  // 0.5秒
}
```

---

### 场景 3: 希望一修改就保存

```json
{
  "files.autoSave": "onFocusChange"
}
```

**效果**: 切换到其他文件时自动保存当前文件。

---

### 场景 4: 添加更多 Markdown 配置

```json
{
  // ... 其他配置 ...

  // 禁用拼写检查（中文小说用不上）
  "cSpell.enabled": false,

  // 禁用 Markdown lint（写作时太严格）
  "markdownlint.config": {
    "default": false
  },

  // 自定义字体（更适合中文阅读）
  "editor.fontFamily": "Source Han Sans CN, 'Microsoft YaHei', monospace",
  "editor.fontSize": 16
}
```

---

## ⚠️ 注意事项

### 1. 配置作用域

`.vscode/settings.json` 是**项目级配置**，优先级高于全局配置。

**示例**:
- 全局配置: `"files.autoSave": "off"`
- 项目配置: `"files.autoSave": "afterDelay"`
- **实际生效**: 项目配置（自动保存开启）

### 2. 配置冲突

如果你已有 `.vscode/settings.json`，Noveler 会询问是否合并配置。

**建议**:
- 选择"合并"：Noveler 推荐配置 + 你的现有配置
- 选择"跳过"：保持现有配置不变

### 3. Git 版本控制

**推荐**: 将 `.vscode/settings.json` 加入版本控制

**原因**:
- 团队协作时，大家使用相同的编辑器配置
- 换电脑/重新克隆项目时，配置自动恢复

**如不想提交**:

在 `.gitignore` 中添加:
```
.vscode/
```

---

## 📖 参考资料

### VSCode 官方文档
- [用户和工作区设置](https://code.visualstudio.com/docs/getstarted/settings)
- [自动保存配置](https://code.visualstudio.com/docs/editor/codebasics#_save-auto-save)

### 常用配置项

| 配置项 | 可选值 | 说明 |
|--------|--------|------|
| `files.autoSave` | `off`, `afterDelay`, `onFocusChange`, `onWindowChange` | 自动保存时机 |
| `files.autoSaveDelay` | 数字（毫秒） | 自动保存延迟 |
| `files.trimTrailingWhitespace` | `true`/`false` | 是否删除行尾空格 |
| `files.insertFinalNewline` | `true`/`false` | 文件末尾是否添加换行 |

---

## ❓ 常见问题

### Q: 初始化时没有创建 `.vscode/settings.json`?

**A**: 可能是选择了"跳过"。手动创建:

1. 在项目根目录创建 `.vscode` 文件夹
2. 创建 `settings.json` 文件
3. 复制 `templates/vscode-settings.json` 的内容

### Q: 我不想要自动保存，但每次初始化都会创建?

**A**: 初始化后删除 `.vscode/settings.json` 中的自动保存配置即可:

```json
{
  // 删除或注释这两行
  // "files.autoSave": "afterDelay",
  // "files.autoSaveDelay": 2000,

  // 保留其他配置
  "files.trimTrailingWhitespace": true
}
```

### Q: 能否禁用这个功能?

**A**: 可以。Noveler 会在初始化时询问，选择"跳过"即可。

---

**祝写作愉快！📝**
